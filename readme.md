# NOTO the (eventualally) open-source collabourative text editor
This is a side project so this is right now just for fun

# How to build
1. Run ``npm run build`` to build the client side typescript files with webpack
2. Then run ``npm start`` to start the server
3. Navigate to ``localhost:8080`` in the browser to see the page